name: Docker Image CI

on:
  #push:
   # branches: [ "master" ]
  #pull_request:
  #  branches: [ "master" ]
  workflow_dispatch:
        
    
    
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - run: ls
    - name: Build the Docker image
      run: docker build --f productoin.Dockerfile --t raphel2b3/swarmintelligencebackend:$(date +%s)
